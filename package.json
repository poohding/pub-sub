{
  "name": "pub-sub",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf .cache dist",
    "start": "run-s clean && parcel src/html/map.html",
    "build": "run-s clean && parcel build src/html/map.html --out-dir build --public-url ./"
  },
  "devDependencies": {
    "@babel/core": "^7.1.2",
    "autoprefixer": "^9.3.1",
    "eslint": "^4.19.1",
    "node-sass": "^4.9.4",
    "npm-run-all": "^4.1.3",
    "parcel-bundler": "^1.10.3",
    "parcel-plugin-eslint": "^1.0.4",
    "rimraf": "^2.6.2"
  }
}
